import { ZoominDirective } from './zoomin.directive';

describe('ZoominDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoominDirective();
    expect(directive).toBeTruthy();
  });
});
